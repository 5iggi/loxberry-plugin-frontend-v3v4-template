# CSS Guide

- Always use a unique scope, e.g. `.plugin-page`.
- Protect custom controls from jQuery Mobile with `data-role="none"`.
- Use LoxBerry v4 tokens with v3 fallbacks.
- PrimeIcons can be used in buttons with `<i class="pi pi-check"></i>`.
